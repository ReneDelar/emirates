---
interface Props { items: string[] }
const { items } = Astro.props;
---
<div class="flex flex-wrap gap-[12px]" data-grid>
  {items.map((p) => (
    <span class="bg-chip text-brand border border-[#d6e6f5] rounded-[22px] px-[17px] py-2 text-[14px] font-semibold">{p}</span>
  ))}
</div>
